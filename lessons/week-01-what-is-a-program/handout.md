# Week 1 — What Is a Program?

**Name: ______________________ Date: __________**

> A **program** is a list of exact instructions a computer follows, in order.

## Today's words

| Word | What it means |
|------|---------------|
| **program** | a list of instructions the computer follows, in order, exactly |
| **code** | the written-out instructions |
| **`print()`** | the command that displays something on the screen |
| **string** | text in quotes — the computer repeats it without doing math |
| **bug / error** | a mistake in code; the computer points at it with a message |

## 1 · Be the computer 🤖

You are the computer. Follow the code *exactly* and write what the screen would show.

```python
print("Welcome!")
print(3 + 4)
print("3 + 4")
```

The screen shows:

> Line 1: ______________________
>
> Line 2: ______________________
>
> Line 3: ______________________

## 2 · Spot the bug 🐞

Each line has **one** mistake. Circle it.

```python
1.  Print("hello")
2.  print("hello)
3.  print(hello there)
```

## 3 · True or false?

Circle one for each:

- The computer can rearrange your instructions into a better order. **T / F**
- `print(2 + 2)` and `print("2 + 2")` show the same thing. **T / F**
- An error message means the computer is telling you where it got stuck. **T / F**

---

## 🧠 Brain teaser (optional — take it home)

**Program a human!** Write instructions for drawing a simple house (square +
triangle roof + door) so *exact* that someone who has never seen a house could
follow them and get it right. Rules:

- Number each step.
- Each step can only be: draw a straight line, or move your pencil without drawing.
- You may use words like up/down/left/right and lengths in centimeters.

Test it on a family member — they must follow it *literally*, like the sandwich robot.
Bring your program (and their drawing!) next week for a shout-out.
